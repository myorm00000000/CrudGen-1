<!DOCTYPE html>
<html>
    <head>
        <meta http-equiv="Content-Type" content="text/html; charset=utf-8" />
        <title>Back Office</title>
        {{ "{% block stylesheets %}" }}
            <link rel="stylesheet" href="{{ "{{ asset('/bundles/sachoocrudgen/css/reset.css') }}" }}" />
            <link rel="stylesheet" href="{{ "{{ asset('/bundles/sachoocrudgen/css/main.css') }}" }}" />
        {{ "{% endblock %}" }}
        <link rel="shortcut icon" href="{{ "{{ asset('favicon.ico') }}" }}" />
    </head>
    <body>

        <section id="wrapper">
            <header id="header">
            	<h1 id="logo"><a href="">Your Logo</a></h1>  
            </header>
            
            {{ "{% block navigation %}" }}
                <nav>
	                <ul id="menu">
	                    <li><a href="#">Menu Link</a></li>
	                </ul>
                </nav>
            {{ "{% endblock %} " }}
            <section class="main-col">
	            
                {{ "{% block content %}" }}          
                {{ "{% endblock %}" }}
               
            </section>

            <div id="footer">
                {{ "{% block footer %}" }}
                {{ "{% endblock %}" }}
            </div>
        </section>
        {{ "{% block javascripts %}" }}
            <script type="text/javascript" src="{{ "{{ asset('/bundles/sachoocrudgen/js/jquery-1.6.4.min.js') }}" }}"></script>
        {{ "{% endblock %}" }}
    </body>
</html>